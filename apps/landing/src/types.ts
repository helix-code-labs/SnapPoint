import type { LucideIcon } from 'lucide-react';

export type Theme = {
    id: string;
    name: string;
    label: string;
    primary: string;
    secondary: string;
    textPrimary: string;
    textSecondary: string;
    bgGradient: string;
    icon: LucideIcon;
    accent: string;
    btnColor: string;
};
