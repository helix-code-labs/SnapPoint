{
  "name": "@snappoints/merchant-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "cf:deploy": "wrangler deploy --env production --config wrangler.toml",
    "cf:deploy:preview": "wrangler deploy --env preview --config wrangler.toml",
    "cf:dry": "wrangler deploy --dry-run --env production --config wrangler.toml"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
